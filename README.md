# Tangelo Protractor Tests

Protractor tests for Tangelo's angular frontend.

####How to run:

- Make sure Tangelo is running
- Install dependencies with npm:

```bash
npm install
```

- Run the following npm command:

```bash
npm test
```
